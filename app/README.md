# Workout-Tracker-App API

### Commands (Local)
#### build
```sh
docker-compose -f local.yml build
```
#### run bash
```sh
docker-compose -f local.yml run --rm app bash
```
#### run tests using pytest
```sh
docker-compose -f local.yml run --rm app pytest
```
